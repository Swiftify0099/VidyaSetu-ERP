/**
 * PremiumButton — Compatibility wrapper for AppButton
 */
import AppButton, { AppButtonProps, ButtonVariant, ButtonSize } from './AppButton';

export type { AppButtonProps as PremiumButtonProps, ButtonVariant, ButtonSize };
export default AppButton;
