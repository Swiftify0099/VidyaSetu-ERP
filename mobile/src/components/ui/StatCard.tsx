/**
 * StatCard — Compatibility wrapper for AppStatCard
 */
import AppStatCard, { AppStatCardProps } from './AppStatCard';

export type { AppStatCardProps as StatCardProps };
export default AppStatCard;
