/**
 * PremiumCard — Compatibility wrapper for AppCard
 */
import AppCard, { AppCardProps, CardVariant } from './AppCard';

export type { AppCardProps as PremiumCardProps, CardVariant };
export default AppCard;
