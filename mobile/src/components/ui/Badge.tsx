/**
 * Badge — Compatibility wrapper for AppBadge
 */
import AppBadge, { AppBadgeProps, BadgeVariant, BadgeSize } from './AppBadge';

export type { AppBadgeProps as BadgeProps, BadgeVariant, BadgeSize };
export default AppBadge;
