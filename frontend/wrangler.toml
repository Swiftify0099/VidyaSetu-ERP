name = "vidyasetu-erp"
compatibility_date = "2024-09-23"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
# Exclude /api/* and /storage/* from the Worker — these must reach the backend.
# The Worker only serves the frontend SPA; API calls go cross-origin to Render.
